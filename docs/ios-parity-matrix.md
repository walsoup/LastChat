# iOS parity matrix

Android remains the acceptance reference. A row is complete only after both a
functional check and a screenshot comparison on compact and wide layouts.

| Surface | Shared/iOS foundation | Production data | Visual QA | Status |
|---|---:|---:|---:|---|
| App theme, shape, and font tokens | Shared shapes, typography, AMOLED transform, same Google Sans Flex TTF, and all six Android palettes | Persisted theme and system/light/dark mode | Pending Xcode | In progress |
| Chat shell and composer | Shared production grouped-bubble container, 48 dp add Surface, baseline row, 24 dp input capsule, outline tokens, five-line text geometry, nine-state Picker/Send/Loading/STT/questionnaire/tool action renderer, exact composer attachment strip, sent-message attachment row/tiles, and assistant-message TTS action used by Android and iOS | Persistent iOS conversations, attachment-only/text sends, native file selection, streaming, cancellation, TTS playback, and empty-state provider-settings handoff | Pending Xcode; STT and remaining tool controllers remain platform gaps | In progress |
| Conversation menu | Shared Android modal drawer sheet, expandable search control, grouped quick-action rows and search animation, circular action Surface/press physics, BarChart/Settings glyphs, footer pill geometry, assistant picker sheet, selected paint, grouped corner animation, and row spacing | App-container persistence, title filtering, select/create/rename/delete, drawer lifecycle, statistics/settings navigation, and assistant-to-conversation handoff | Pending Xcode; Imagine remains until iOS image generation is functional | In progress |
| Top-app-bar navigation | Shared Android back control plus the outlined 48 dp chat drawer control and rounded Menu glyph | Native route callbacks and Pop haptic semantics | Pending Xcode | In progress |
| Statistics | Shared production stat-card and activity-heatmap implementations | Persisted conversation, message, input, output, cached-token, and per-day activity totals | Pending Xcode | In progress |
| Settings shell | Shared production compact groups, input cards, form rows, Android's exact compact Providers/Search/TTS selector, and its 336 dp adaptive pane at the 840x600 breakpoint, including full compact/wide hierarchy, expandable children, selected paint, corner morphing, row heights, press physics, and timing | Provider, search, appearance, multiple assistants, system prompt, and Keychain settings persist; unported destinations show explicit unavailable details | Pending Xcode; many individual detail destinations remain unported | In progress |
| About | Shared Android production body and exact launcher asset, including logo footprint, version pill, grouped rows, press spring, and credits | Live platform version, device, CPU architecture, source URL opening, and platform haptics | Pending Xcode | In progress |
| Default model and provider configuration | Shared production model-feature card and grouped picker rows, including animated selected-pill geometry | Three chat providers, retained per-provider endpoint/model settings, searchable persisted default-model selection, active generation routing, and per-provider Keychain secrets | Pending Xcode; provider icons and the full Android catalog/favorites picker remain incomplete | In progress |
| Streaming generation | Real providers plus Darwin HTTP/SSE | OpenAI-compatible, Google, and Claude chat wired; cancellation, 1-second persistence checkpoints, and the standard model-driven tool-call/result loop with a 256-step ceiling | No | In progress |
| Attachments and media | Native media/document picker, sandbox copy, provider media encoder, Coil image rendering, shared production 84 dp composer strip, shared full-width sent-message row with 72 dp tiles, and portable attachment-opening contract | Pending drafts and sent media persist; UIKit preview/Open In is wired for sent tiles; Android crop/URI cleanup, archived grayscale, zoom, and intents remain adapter-owned; PDF/DOCX prompt parsing and inline media controls pending | Pending Xcode | In progress |
| Memory and RAG | Production memory mode/settings/row UI, temporal extraction contract/prompt, vector math, keyword scoring, and chunking are source-shared with Android | Persistent assistant-scoped core/episodic memories; Basic and hybrid Searchable recall; Adaptive evidence watermarks, 8-message/10-minute extraction trigger, structured claim lifecycle and episode upserts; native iOS BGAppRefresh durability with in-process fallback; OpenAI/Google embeddings; memory tools; preferences contain no secrets | Pending Xcode scheduling simulation and live-provider extraction QA | In progress |
| Search providers | All services compile for iOS; Bing's portable client is installed and the real `search_web` tool is wired | Bing plus 13 API-backed providers have persistent settings, Keychain-only secrets, configurable result limits, and model-driven execution; SearXNG advanced URL/auth configuration remains | Pending Xcode | In progress |
| Cloud TTS | All eight cloud providers, source-shared queue/chunk/prefetch controller, and AVFoundation audio player compile for iOS; Android keeps Media3 behind the same playback contract | Provider-specific settings persist without credentials, API keys remain Keychain-only, and assistant-message play/stop uses the same production action as Android | Pending Xcode playback and screenshot QA | In progress |
| Local assistant tools | Android's production settings-group rows, normal model tool loop, questionnaire UI, and exact image-tool schema/system guidance are reused | Notification/TTS/scheduled/history/questionnaire paths are functional; OpenAI-compatible and Google `generate_image` calls use the shared production providers, persist decoded media metadata, return Android-shaped gallery JSON, and render returned markdown as chat image attachments | Pending Xcode interaction/background/image QA; QuickJS and ComfyUI configuration remain | In progress |
| Image generator and gallery | Android's Imagine drawer entry, generator/gallery hierarchy, crossfade, empty state, 28 dp floating input surface, 20 dp media corners, aspect/count settings, and stop/generate interaction are reproduced in Compose Multiplatform | OpenAI-compatible/Google generation, cancellation, app-container persistence, preview opening, gallery grid, and deletion are functional | Pending Xcode animation/screenshot QA; image-to-image and ComfyUI workflow configuration remain | In progress |
| Local models/workspace | Android implementation | No | No | Pending |
| Backup and restore | Android implementation | No | No | Pending |

## Acceptance rules

- Do not change Android spacing, typography, colors, shapes, motion, or haptics
  merely to accommodate iOS.
- Use Compose Multiplatform for shared UI. SwiftUI is only the native host.
- Keep platform-only capabilities behind contracts and show an explicit
  unavailable state until the iOS adapter exists.
- Never store provider secrets in preferences; the iOS implementation must use
  Keychain before provider configuration is enabled.
- Validate the Android app after every common UI extraction.
